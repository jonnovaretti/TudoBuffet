<div class="col-lg-3">
    <div class="card sidebar-menu">
        <div class="card-header">
            <h3 class="h4 card-title">Seção do usuário</h3>
        </div>
        <div class="card-body">
            <ul class="nav nav-pills flex-column">

                @if (Context.Request.Path.HasValue && Context.Request.Path.Value.Equals("/admin/buffets"))
                {
                    <a href="~/admin/buffets" class="nav-link active"><i class="fa fa-plus"></i> Novo buffet</a>
                }
                else
                {
                    <a href="~/admin/buffets" class="nav-link"><i class="fa fa-plus"></i> Novo buffet</a>
                }

                @if (Context.Request.Path.HasValue && Context.Request.Path.Value.Equals("/admin"))
                {
                    <a href="~/admin" class="nav-link active"><i class="fa fa-list"></i> Meus buffets</a>
                }
                else
                {
                    <a href="~/admin" class="nav-link"><i class="fa fa-list"></i> Meus buffets</a>
                }

                @if (Context.Request.Path.HasValue && Context.Request.Path.Value.Equals("/admin/budgets"))
                {
                    <a href="~/admin/budgets" class="nav-link active"><i class="fa fa-list"></i> Orçamentos recebidos</a>
                }
                else
                {
                    <a href="~/admin/budgets" class="nav-link"><i class="fa fa-list"></i> Orçamentos recebidos</a>
                }


                @if (Context.Request.Path.HasValue && Context.Request.Path.Value.Equals("/admin/account"))
                {
                    <a href="~/admin/account" class="nav-link active"><i class="fa fa-list"></i> Minha conta</a>
                }
                else
                {
                    <a href="~/admin/account" class="nav-link"><i class="fa fa-list"></i> Minha conta</a>
                }

                <a href="index.html" class="nav-link"><i class="fa fa-sign-out"></i> Sair</a>
            </ul>
        </div>
    </div>
</div>